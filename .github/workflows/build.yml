name: CI build

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Deploy Docker Image to Docker Hub.
      run: echo "Mock a deployment."
      
